import { router } from "@inertiajs/svelte";
// import { setAxiosAuthorizationHeader } from '../bootstrap';

export default function persistTokenAndRedirect(apiToken) {
    // if (apiToken) {
    //     sessionStorage.setItem('api_token', apiToken);
    //     setAxiosAuthorizationHeader(apiToken);
    //     router.visit('/');
    // }
}
